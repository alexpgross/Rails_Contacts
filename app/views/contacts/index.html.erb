<h1 class="lobster_font">Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact %></li>
  <% end %>
</ul>

<p><%= link_to 'Create Contact', new_contact_path %></p>